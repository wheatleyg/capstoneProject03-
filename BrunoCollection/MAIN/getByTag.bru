meta {
  name: getByTag
  type: http
  seq: 2
}

get {
  url: http://{{host}}/api/getByTag/{{tag}}?count={{count}}
  body: none
  auth: inherit
}

vars:pre-request {
  tag: galaxy
  count: 1
}

settings {
  encodeUrl: true
  timeout: 0
}
