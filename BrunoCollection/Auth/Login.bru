meta {
  name: Login
  type: http
  seq: 2
}

post {
  url: http://{{host}}/api/auth/login
  body: json
  auth: inherit
}

body:json {
  {
    "Username":"TestUser",
    "Password": "GoFlashes.2025"
  }
}

script:post-response {
  let response = res.body;
  bru.setEnvVar("auth_token", response.token)
}

settings {
  encodeUrl: true
  timeout: 0
}
